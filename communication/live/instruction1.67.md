# SMART BUSINESS MISSION CONTROL

## SB-P-1.11-ID-2 — CONTROLLED FOUNDER IDENTITY & BUSINESS CONTINUITY EXECUTION

**Mission ID:** SB-P-1.11-ID-2  
**Mission Name:** Controlled Founder Identity & Business Continuity Execution  
**Mission Status:** ACTIVE  
**Authorized By:** Mission Control  
**Execution Basis:** `communication/live/report1.72.md`

---

## 1. Mission Decision

Mission Control accepts the `report1.72.md` recommendation to use **Strategy B**:

> Establish `iam.mrriyas@gmail.com` as a fresh canonical Auth identity in production through the ordinary user-facing authentication flow, then create the Founder business workspace through the application's existing self-service business setup flow.

No legacy Auth UUID preservation is required.

No privileged Auth write, service-role user creation, raw `auth.users` write, manual business-row insertion, or ownership remap is authorized.

### Founder decision — legacy transaction

The single legacy transaction — ₹37, item `Soap`, customer `Rafi` — is classified:

**`RETAIN LEGACY ONLY`**

It shall **not** be re-entered into canonical production.

Reason:

- it is the only dependent legacy row;
- it has no demonstrated accounting or continuity requirement;
- re-entering it would introduce historical/test-looking data into the clean canonical workspace;
- preserving it in the legacy system maintains historical evidence without polluting canonical production.

This decision closes the only Founder decision requested by `report1.72.md`.

---

## 2. Mission Objective

Establish a clean canonical Founder identity and Founder-owned business workspace using only normal application flows, verify Owner isolation and workspace continuity, and unblock resumption of `SB-P-1.11-CP-1` Founder preview.

This is a narrow continuity execution mission.

---

## 3. Locked Systems

Canonical production Supabase:

`gysgzasfcjvtrgaigfyn`

Authorized Lovable preview project:

`f3e992ec-06df-4d49-b157-b92ec064c078`

Founder canonical email:

`iam.mrriyas@gmail.com`

Legacy Lovable Cloud remains read-only and historical only.

---

## 4. Preflight — Read Only

Before any user action:

1. Verify production Auth still does not contain `iam.mrriyas@gmail.com`.
2. Verify production contains zero business rows.
3. Verify no canonical business already semantically represents the Founder workspace.
4. Verify the preview app is still bound to production Supabase `gysgzasfcjvtrgaigfyn`.
5. Verify the normal signup and business-setup flows are present and unchanged.

If any material assumption differs from `report1.72.md`, STOP.

---

## 5. Founder Human Action — Canonical Signup

The Founder shall create the canonical account through the application's ordinary public authentication/signup flow using:

`iam.mrriyas@gmail.com`

Requirements:

- use the supported user-facing signup method exposed by the current app;
- complete any email confirmation or normal authentication verification required by Supabase;
- do not use Supabase Dashboard `Add user`;
- do not use Admin API/service role;
- do not attempt to preserve legacy UID `74d920ee-b736-4c25-aef2-13bf2e5cff62`;
- accept the new canonical Auth UUID assigned by Supabase.

If signup cannot complete through the normal user-facing flow, STOP and report the exact blocker. Do not substitute an administrative workaround.

---

## 6. Founder Human Action — Business Setup

After successful canonical authentication, use the application's existing Dashboard/business-setup form to create the Founder business workspace.

Use the real current business identity details the Founder intends to keep in canonical production.

Do not manually copy legacy IDs.

The new business row must:

- receive a new canonical business UUID generated by the application/database;
- set `owner_id` to the newly authenticated canonical Founder Auth UUID through the existing normal flow;
- contain only intended canonical business identity data;
- contain no migrated legacy transaction/history data.

The legacy ₹37 transaction remains legacy-only.

---

## 7. Post-Execution Verification — Read Only

After the Founder completes the two normal user actions, independently verify:

1. exactly one canonical Auth user exists for `iam.mrriyas@gmail.com`;
2. its provider/sign-in method matches the method actually used;
3. exactly one Founder-owned business row exists for the new canonical Founder UID;
4. the Founder can load the authenticated Dashboard;
5. the Founder sees only the Founder-owned business workspace;
6. another canonical user cannot read or mutate the Founder business through Owner-scoped paths;
7. business RLS remains enabled and unchanged;
8. no legacy UUID was inserted into canonical Auth or business ownership;
9. no legacy transaction was copied;
10. no unrelated production table received rows as a side effect except rows normally created by signup/business setup;
11. Catalog, Inventory, and authenticated navigation are reachable from the Founder workspace as expected;
12. no service-role/admin/manual production write occurred.

If any ownership or isolation mismatch is found, STOP immediately and do not continue Founder preview.

---

## 8. CP-1 Resumption Gate

`SB-P-1.11-CP-1` may resume only if all continuity checks above PASS.

On PASS:

- the existing `AWAITING FOUNDER ACCEPTANCE` state in `report1.71.md` remains valid;
- Founder may reopen the authorized Lovable preview and perform the visual/responsive/keyboard acceptance walkthrough;
- no publish/deploy/domain authorization is implied.

---

## 9. Required Evidence

Create:

`communication/live/report1.73.md`

The report must record:

- preflight state;
- signup method used;
- canonical Founder Auth UID assigned;
- canonical Founder business ID assigned;
- business identity created;
- confirmation that the legacy ₹37 transaction was not migrated;
- RLS/isolation verification;
- tables/row counts affected by the normal flows;
- CP-1 resumption verdict.

Final verdict must be one of:

- `PASS — CP-1 MAY RESUME`
- `STOPPED — USER-FLOW BLOCKER`
- `STOPPED — CONTINUITY VERIFICATION FAILURE`
- `FAIL`

---

## 10. Hard Boundaries

Do not:

- preserve or inject the legacy Auth UUID;
- write directly to `auth.users`;
- create users with Admin API/service role;
- manually insert/update the production `businesses` row;
- migrate the legacy ₹37 transaction;
- migrate the unrelated Salamath Store business;
- mutate the legacy Lovable Cloud backend;
- alter RLS, schema, functions, migrations, grants, or Catalog commands;
- add a twentieth public Catalog command;
- connect Lovable to GitHub;
- enable Lovable Cloud on the authorized preview project;
- publish;
- deploy;
- cut over `smartbusiness.teamlips.com`.

Any unexpected blocker must be reported rather than worked around by expanding authority.

---

## 11. Completion

After successful continuity verification:

1. create `communication/live/report1.73.md`;
2. run repository and Markdown quality gates;
3. open one completion PR;
4. state whether `SB-P-1.11-CP-1` may resume;
5. stop.

Do not self-merge.

---

## Next logical step

Founder completes canonical signup and business setup through the normal application flow under this authorization, then the continuity evidence is verified and `SB-P-1.11-CP-1` resumes if PASS.
