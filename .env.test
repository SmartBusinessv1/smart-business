# SB-MIG-1.2E-B — dedicated, genuinely isolated test-only Supabase project.
# This is NOT the application's runtime backend and is NOT the Team LIPS
# production project. Project drravyyauixltoihzmwo ("smart-business-test",
# org "Smart Business Testing", Free plan) is a separate Supabase
# organization/account created specifically so the automated test suite
# (npm run test) can never reach production data or credentials.
# See docs/migration/SB-MIG-1.2E/11-test-environment-isolation.md.
SUPABASE_TEST_URL=https://drravyyauixltoihzmwo.supabase.co
SUPABASE_TEST_ANON_KEY=sb_publishable__2zHmm-kEbI_doT8pPeS0w_DsWLwr1A

# SUPABASE_TEST_SERVICE_ROLE_KEY is intentionally NOT here — it goes in
# .env.test.local (gitignored via the repo's existing *.local rule) and is
# never committed. See that file's own header comment for where to get it.
