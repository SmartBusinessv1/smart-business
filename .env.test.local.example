# Copy this file to .env.test.local (gitignored, never commit) and fill in
# the real value. Get it from: Supabase dashboard -> project
# drravyyauixltoihzmwo ("smart-business-test", org "Smart Business Testing")
# -> Project Settings -> API Keys -> "Secret keys" -> reveal the default key.
#
# This project is a dedicated, separate Supabase organization used only for
# automated testing -- it is NOT the Team LIPS production project and NOT
# connected to it in any way. See
# docs/migration/SB-MIG-1.2E/11-test-environment-isolation.md.
#
# Used only by tests/setup/test-clients.ts (the adminClient) to
# provision/manage test-only Supabase Auth users on the dedicated test
# project. Never used against the application's real runtime backend.
SUPABASE_TEST_SERVICE_ROLE_KEY=
