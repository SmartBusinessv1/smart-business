# Copy this file to .env.test.local (gitignored, never commit) and fill in
# the real value. Get it from: Supabase dashboard -> project gysgzasfcjvtrgaigfyn
# -> Project Settings -> API -> "service_role" secret key.
#
# Used only by tests/setup/global-setup.ts to provision/manage test-only
# Supabase Auth users on the dedicated test project. Never used against the
# application's real runtime backend.
SUPABASE_TEST_SERVICE_ROLE_KEY=
