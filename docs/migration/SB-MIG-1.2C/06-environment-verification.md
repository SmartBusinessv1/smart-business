Document: Environment Verification

Version: 1.0

Status: DRAFT — submitted for Mission Control review

Created By: Claude Code

Reviewed By: Mission Control (pending)

Mission: SB-MIG-1.2C

# SB-MIG-1.2C — Environment Verification (Task 6)

Names only — no values are reproduced anywhere in this document, matching the discipline established in every prior mission's environment-variable reporting (most recently `SB-MIG-1.2A/06-environment-cutover-map.md`).

## 1. Application Runtime Variables

| Variable | Required | Configured (current, tracked `.env`) | Destination | Verification status |
| --- | --- | --- | --- | --- |
| `SUPABASE_URL` | Yes — server-side (`auth-middleware.ts`, `client.server.ts`) | Yes | Currently: current production Supabase project. Target: Team LIPS Supabase (`https://gysgzasfcjvtrgaigfyn.supabase.co`, confirmed via `get_project_url`) | **Verified present; verified pointed at the pre-cutover project (expected — no cutover authorized this mission)** |
| `SUPABASE_PUBLISHABLE_KEY` | Yes — server-side (`auth-middleware.ts`) | Yes | Same as above | Verified present |
| `VITE_SUPABASE_URL` | Yes — client-side, build-time (`client.ts`) | Yes | Same as above, baked in at build time | Verified present |
| `VITE_SUPABASE_PUBLISHABLE_KEY` | Yes — client-side, build-time (`client.ts`) | Yes | Same as above | Verified present |
| `SUPABASE_SERVICE_ROLE_KEY` | Declared by `client.server.ts`, but **not called by any live code path** (`05-secret-management-report.md` §1) | **Not present in any tracked file** (correct — this is a secret, and no live code path currently exercises it) | Undocumented Lovable-side runtime mechanism today; would need to be Team LIPS Supabase's own service-role value post-cutover | Verified absent from the repository (correct); production injection mechanism unverified (`05-secret-management-report.md` §3) |
| `SUPABASE_PROJECT_ID` | No — confirmed unreferenced anywhere in `src/` (re-confirmed this mission; matches SB-MIG-1.1 Finding ENV-1) | Yes, declared in `.env` | N/A — unused by code | Verified present but inert; no action needed |
| `VITE_SUPABASE_PROJECT_ID` | No — same as above | Yes, declared in `.env` | N/A — unused by code | Verified present but inert; no action needed |

## 2. Test-Suite Variables

| Variable | Required | Configured | Destination | Verification status |
| --- | --- | --- | --- | --- |
| `SUPABASE_TEST_URL` | Yes — `tests/setup/*` | Yes, tracked `.env.test` | Team LIPS Supabase (already the correct, final target — no change needed) | Verified present, verified correctly targeted |
| `SUPABASE_TEST_ANON_KEY` | Yes | Yes, tracked `.env.test` | Same | Verified present |
| `SUPABASE_TEST_SERVICE_ROLE_KEY` | Yes — test user provisioning/teardown | Yes, **untracked** `.env.test.local` (name-only placeholder in the tracked `.env.test.local.example`) | Same | Verified present locally; verified correctly gitignored and never staged |

## 3. Deployment-Config Files (Informational, Not Currently Active)

| File | Status | Note |
| --- | --- | --- |
| `.output/server/wrangler.json` | Build artifact, **not tracked in git**, regenerated by `npm run build` | Cloudflare Workers deployment config, generated by the Nitro build target this repository's TanStack Start config supports. Contains no secrets itself (Cloudflare secrets are configured separately via `wrangler secret put` or the Cloudflare dashboard, never written to this file). **Not an active deployment path** — this mission's Locked Decision keeps the app hosted through Lovable, so this file is currently inert build output, not a live configuration surface. Flagged for completeness only. |

## 4. New or Deprecated Variables This Mission

None. This mission introduces no new variable name and deprecates none — consistent with SB-MIG-1.2A's `06-environment-cutover-map.md` §4–5 finding that the environment footprint is fully enumerated and stable.

## 5. Summary

| Requirement | Status |
| --- | --- |
| Every variable named | Yes — 10 variables across application runtime (7) and test suite (3) |
| Every variable's required/configured/destination state recorded | Yes |
| No value exposed | Confirmed — this document and every prior mission document this session follow the same discipline |
| Cutover-relevant variables identified | Yes — `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (5 variables, matching `SB-MIG-1.2A/06-environment-cutover-map.md` §1's count exactly) still require a value change at actual cutover time — not performed by this mission (no cutover authorized) |
