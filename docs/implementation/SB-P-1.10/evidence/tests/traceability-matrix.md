# SB-P-1.10-TESTS-1.0 / SB-P-1.10-FIX-IDEMPOTENCY-RLS-1.0 — Engineering Contract §16 Traceability Matrix

Maps every Engineering Contract §16 testing obligation (`docs/implementation/SB-P-1.10/engineering-contract.md`, Version 1.3) and its corresponding EIS §15 line to the automated test(s) that exercise it, the result of the final (post-corrective-mission) run, and supporting evidence. Raw output: `test-run-output.txt`. Execution details: `test-summary.md`.

**Updated 24 July 2026 under SB-P-1.10-FIX-IDEMPOTENCY-RLS-1.0.** Originally authored under SB-P-1.10-TESTS-1.0 with 4 Fail rows against a discovered defect (`DEFECT-idempotency-select-for-update-rls.md`); that defect has since been corrected under a separate, explicitly authorized corrective mission, and this matrix now reflects the current, passing result. The original per-row Fail rationale is preserved in each row's Notes column rather than deleted, so the history of what failed and why remains visible.

All tests run against the dedicated test-only Supabase project `gysgzasfcjvtrgaigfyn` (not the production Lovable-managed backend), with the unmodified `businesses` + SB-P-1.10 inventory schema migrations applied. See `test-summary.md` for exactly which migrations and why.

| # | Contract §16 obligation | Test file(s) | Key test name(s) | Result | Notes |
| - | --- | --- | --- | --- | --- |
| 1 | Ledger correctness across opening stock, increases, decreases, adjustments, corrections | `ledger-correctness.test.ts` | "accumulates opening stock, increases, decreases, adjustments, and a correction to the correct running total" | **Pass** | Verified against an independent recomputation from raw movement history, not just the RPC's own return value |
| 2 | Independent permission testing (view, opening-stock, adjustment, correction) | `permissions.test.ts` | "view:...", "opening-stock:...", "adjustment:...", "correction:...", "unauthenticated caller...", "archive... is owner-only" | **Pass** | SB-P-1.10 is Owner-only (no secondary role table exists); "authorized" = business owner, "unauthorized" = any other signed-in user or anon |
| 3 | RLS cross-business access tests, including non-disclosure | `rls-cross-business.test.ts` | "owner B's unfiltered list...", "owner B querying owner A's item by its known id gets nothing, not a 403", "...never includes owner A's rows", "cannot insert...", "anon caller sees nothing..." | **Pass** | Explicitly checks non-disclosure: a cross-business lookup by known id returns `null`, not a distinguishable "forbidden" response |
| 4 | Correction tests: original-movement immutability, correction linkage, independent correction-permission | `correction-behaviour.test.ts` | "original movement is byte-for-byte unchanged...", "an ordinary owner's UPDATE attempt never mutates the row...", "defence-in-depth: the append-only trigger itself rejects...", "correction permission is enforced independently..." | **Pass** | See note below on the UPDATE-rejection mechanism — this obligation and #6 (append-only) share a discovery |
| 5 | Audit-integrity: no movement without responsible-user or event reference | `audit-integrity.test.ts` | "every movement created through the shared write path carries the caller...", "a direct insert with neither... is rejected...", "a direct insert with only a business_event reference... is accepted" | **Pass** | Enforced by the `inventory_movements_audit_completeness` CHECK constraint, confirmed independent of the RPC path |
| 6 | Append-only enforcement | `correction-behaviour.test.ts` (UPDATE tests); `archived-item-protection.test.ts`, `cross-business-consistency.test.ts` (incidental UPDATE/DELETE absence) | "an ordinary owner's UPDATE attempt never mutates the row...", "defence-in-depth: the append-only trigger itself rejects mutation..." | **Pass** | **Finding, not a defect:** `inventory_movements` has no RLS UPDATE/DELETE policy at all, so an ordinary owner's mutation attempt is silently blocked by RLS's default-deny (0 rows matched, no error) *before* the `BEFORE UPDATE`/`BEFORE DELETE` triggers ever get a matching row to fire on. The triggers are real and were independently confirmed to reject mutation when tested via `service_role` (which bypasses RLS) — genuine defence-in-depth, not dead code, but a different mechanism than "the trigger raises an exception for every caller" |
| 7 | Idempotency replay (matching-payload retries) | `idempotency.test.ts`, `concurrency.test.ts`, `trusted-event-link.test.ts` | "same key + identical payload returns the original movement, not a duplicate"; "a sequential retry with the same key and payload..."; "retried event delivery with the same idempotency key..." | **Pass** (was **Fail** under SB-P-1.10-TESTS-1.0) | **Corrected under SB-P-1.10-FIX-IDEMPOTENCY-RLS-1.0** — see `DEFECT-idempotency-select-for-update-rls.md`, "Resolution" section. Originally: discovered defect, reproduced two independent ways (JS integration test and raw SQL); no fix applied under the test-only mission. Now: re-verified passing, including a direct raw-SQL replay of the exact original reproduction case |
| 8 | Idempotency conflict (mismatched payload rejected) | `idempotency.test.ts` | "same key + different payload is rejected as a conflict, not treated as a retry" | **Pass** (was **Fail**) | Same root cause and fix as #7 |
| 9 | Negative-stock: unauthorized blocking, authorized warning/confirmation, full auditability | `negative-stock.test.ts` | "without explicit authorization... blocked and nothing is persisted", "with explicit authorization... succeeds and is fully auditable" | **Pass** | "Unauthorized" = `allow_negative_stock` not set — SB-P-1.10 has no separate negative-stock role/permission; the explicit flag *is* the authorization mechanism |
| 10 | Performance: acceptable bounds as volume grows; grouped aggregation not N+1 | `performance.test.ts` | "history retrieval stays within 5000ms...", "grouped current-stock aggregation stays within 5000ms and is correct" | **Pass** | Timing assertions at 61 movements (well under bound). The N+1-vs-grouped structural claim is verified separately via `query-plan-evidence.txt` (a single `HashAggregate` query regardless of array size), since a client-side round-trip count cannot observe the server's query plan |
| 11 | Concurrency: correct negative-stock decisions under contention; idempotency-key protection incl. retries | `concurrency.test.ts` | "two concurrent decrease requests that would together go negative: exactly one succeeds", "concurrent requests with the SAME idempotency key produce exactly one movement" | **Pass** (was: Pass for negative-stock race, Fail for idempotency-key race — see #7) | The per-item `pg_advisory_xact_lock` correctly serializes the negative-stock race (verified: exactly 1 of 2 concurrent requests succeeds, final stock is correct) — unchanged by the corrective mission. The idempotency-key-dedup half is now also **Pass**, fixed alongside #7; re-run 3 times in isolation post-fix specifically to stress-test this race, no flakiness observed |
| 12 | Movement type/direction invariant matrix | `movement-type-direction.test.ts` | "accepts valid pairing ...", "rejects invalid pairing ...", "a correction is accepted with either direction..." | **Pass** | All 5 valid and all 5 invalid type/direction pairings individually exercised |
| 13 | Opening-stock invariant (reject 2nd) | `opening-stock-invariant.test.ts` | "a second opening-stock movement for the same item is rejected", "an incorrect opening-stock value is fixed via a linked correction, not a replacement opening-stock row" | **Pass** | |
| 14 | Correction-link integrity: self-reference, cycles, duplicate full reversal, over-compensation | `correction-link-integrity.test.ts` | "cannot reference itself...", "a correction cannot itself be corrected (cycle prevention)", "a duplicate full reversal... is rejected", "partial/multiple compensations are accepted... and rejected once it would exceed it" | **Pass** | Self-reference tested via direct admin-client insert with an explicit id (cannot arise through the RPC path, since ids are server-generated) |
| 15 | Cross-business consistency (DB-level, independent of app validation) | `cross-business-consistency.test.ts` | "application-level: ... fails via RLS...", "database-level, independent of RLS: service-role insert pairing item A with business B is rejected...", "...a correction referencing a movement from a different item/business is rejected..." | **Pass** | The two "database-level" tests deliberately use the `service_role` (RLS-bypassing) client to prove the FK constraints themselves reject it, not just RLS |
| 16 | Archived-inventory write protection: rejection, reactivation, authorized correction | `archived-item-protection.test.ts` | "ordinary movements are rejected against an archived item", "a correction against an archived item's history is explicitly still permitted", "reactivation restores ordinary write access" | **Pass** | |
| 17 | Trusted event-link contract: non-existent/cross-business references, idempotent retried delivery | `trusted-event-link.test.ts` | "business_event_type and business_event_id must both be present or both absent", "a movement with a well-formed (currently unvalidated) event reference is accepted...", "retried event delivery... does not create a duplicate movement" | **Pass**, with a standing scope note (was **Partial / Fail**) | **Scope note (not a defect, unaffected by the corrective mission):** per Engineering Contract §11 and EIS §4, SB-P-1.10 intentionally does not validate that `business_event_id` references a real row — no event-owning domain exists yet; that validation is explicitly assigned to a *future* domain. The pairing CHECK was always **Pass**. The idempotent-retry half was the same defect as #7 — now also **Pass**, corrected under SB-P-1.10-FIX-IDEMPOTENCY-RLS-1.0 |
| — | Shared write-path enforcement (§16 item list in the mission brief / §11 "Shared Movement-Path Obligations") | `shared-write-path.test.ts` | "static check: no application source file under src/ writes to inventory_movements except via the create_inventory_movement RPC", "database-level probe: ... records the actual behavior of a direct insert..." | **Pass** (static check) / **Finding** (DB probe) | **Finding, not corrected here:** the DB-level probe confirms a direct `.insert()` into `inventory_movements`, bypassing `create_inventory_movement()` entirely, **does** succeed and bypasses the negative-stock guard (which lives only in the function body, not a trigger or CHECK constraint). The application code itself never does this (confirmed by the static check), so there is no live exploit path today — but the guarantee is enforced by convention, not by the database, contrary to what "shared write-path enforcement" implies. Reported alongside the idempotency defect, not silently accepted |

## Summary counts

- **Pass:** all 17 of 17 Contract §16 line items, plus the static shared-write-path check.
- **Fail:** none, as of SB-P-1.10-FIX-IDEMPOTENCY-RLS-1.0 (24 July 2026). Previously (under SB-P-1.10-TESTS-1.0): idempotency replay (#7), idempotency conflict (#8), the idempotency half of concurrency (#11), and the idempotent-retry half of trusted event-link (#17) — all one root-cause defect (`DEFECT-idempotency-select-for-update-rls.md`), now corrected and re-verified.
- **Finding (not pass/fail, reported for Mission Control judgment, unaffected by the corrective mission — out of its authorized scope):** append-only enforcement mechanism differs from what the migration comment implies (#6, not a security gap); the shared write path is enforced by RLS/convention rather than by the database at the grant/trigger level for the specific guards inside `create_inventory_movement()` (shared-write-path DB probe).

As of SB-P-1.10-FIX-IDEMPOTENCY-RLS-1.0, this suite considers every Engineering Contract §16 testing obligation satisfied (17/17 Pass), with item 17 carrying a standing scope note (not a defect) and item 6 and the shared-write-path probe carrying findings Mission Control may wish to act on separately — neither blocks §16 itself. See the completion report §5 and §7, and its new corrective-mission appendix, for how this is carried forward.
